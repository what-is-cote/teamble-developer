
  // queue1
  //   .concat(queue2)
  //   .sort((a, b) => b - a)
  //   .forEach((e) => {
  //     if (check[0] + e <= (sum1 + sum2) / 2) check[0] += e;
  //     else check[1] += e;
  //   });

  // if (check[0] !== check[1]) return -1;